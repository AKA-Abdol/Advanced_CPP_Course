#include <iostream>
#include <vector>
#include <string>
#include <cmath>

using namespace std;

// // outputType funcName(input1, input2)
// double vectorLen(double x, double y)
// {
//     return sqrt(x * x + y * y);
// }

// // struct
// struct Vector {
//     double x;
//     double y;
// };

// double vectorLen(Vector v)
// {
//     return sqrt(v.x * v.x + v.y * v.y);
// }

// // typedef
// typedef vector<int> vInt;
// typedef vector<string> vStr;
// typedef long long ll;

int main()
{
    // // function
    // cout << vectorLen(3, 5) << endl;

    // // struct 
    // Vector v;
    // v.x = 3;
    // v.y = 4;
    // cout << vectorLen(v) << endl;
    // cout << v.x << endl;

    // // typedef
    // vInt v;
    // int n = 5;
    // while(n--)
    //     v.push_back(3 * n);
    // cout << v[1] << endl;
    return 0;
}
